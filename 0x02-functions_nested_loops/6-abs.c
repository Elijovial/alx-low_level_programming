#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int n: integer argument
 * Return: absolute value
 */
int _abs(int)
{

	int n;

	if (n > 0)
	{
	return (n);
	}
	else
	{
	return (-n);
	}
	return (0);
}
