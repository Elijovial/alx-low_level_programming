#include <stdio.h>
/**
 * main - print possible combinations of digits
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
int i, j;
for (i = 0; i < 10; ++i)
{
putchar(i + '0');
putchar(',');
putchar(' ')
	putchar(' ');
}
return (0);
}
